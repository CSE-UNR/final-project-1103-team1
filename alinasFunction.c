//call readline function
//call switch
//save into new array (edited)


//this function is going to call the readline function, which has the whole plain madlib in there, then i will call the function with the switch statements to make the new edited madlib then it prints that

#include <stdio.h>
#define CAP 100

void printString(int param1, int param2, char printing_array[][])

//main function and whatver is inbetween

void printString(int param1, int param2, char printing_array[][]){
//call readline
	readLine(FILE *flptr, char arr[][]); //temporary parameters until i see the actual
//call the switch
	userInput(char fig, char word[][cols]); //temporary parameters
	for(int index = 0; index != '\n'; index++){
		readline_array = printing_array;
			printf("%c",printing_array[][]);
	}
	
	//make it so that readline array = switch array
	//then print the switch array into the main when the function is called
}
